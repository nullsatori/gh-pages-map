import { Dialog } from '@mui/material';
import NiceModal, { muiDialog, useModal } from '@ebay/nice-modal-react';

// green regions modals

export const RU_SA = NiceModal.create(() => {
  const modal = useModal();
  return <Dialog {...muiDialog(modal)}> Hello!!! </Dialog>;
});

export const RU_PRI = NiceModal.create(() => {
  const modal = useModal();
  return <Dialog {...muiDialog(modal)}> Hello!!! </Dialog>;
});

export const RU_IRK = NiceModal.create(() => {
  const modal = useModal();
  return <Dialog {...muiDialog(modal)}> Hello!!! </Dialog>;
});

export const RU_KYA = NiceModal.create(() => {
  const modal = useModal();
  return <Dialog {...muiDialog(modal)}> Hello!!! </Dialog>;
});

export const RU_TOM = NiceModal.create(() => {
  const modal = useModal();
  return <Dialog {...muiDialog(modal)}> Hello!!! </Dialog>;
});

export const RU_NVS = NiceModal.create(() => {
  const modal = useModal();
  return <Dialog {...muiDialog(modal)}> Hello!!! </Dialog>;
});

export const RU_KEM = NiceModal.create(() => {
  const modal = useModal();
  return <Dialog {...muiDialog(modal)}> Hello!!! </Dialog>;
});

export const RU_KHM = NiceModal.create(() => {
  const modal = useModal();
  return <Dialog {...muiDialog(modal)}> Hello!!! </Dialog>;
});

export const RU_TYU = NiceModal.create(() => {
  const modal = useModal();
  return <Dialog {...muiDialog(modal)}> Hello!!! </Dialog>;
});

export const RU_SVE = NiceModal.create(() => {
  const modal = useModal();
  return <Dialog {...muiDialog(modal)}> Hello!!! </Dialog>;
});

export const RU_PER = NiceModal.create(() => {
  const modal = useModal();
  return <Dialog {...muiDialog(modal)}> Hello!!! </Dialog>;
});

export const RU_CHE = NiceModal.create(() => {
  const modal = useModal();
  return <Dialog {...muiDialog(modal)}> Hello!!! </Dialog>;
});

export const RU_ROS = NiceModal.create(() => {
  const modal = useModal();
  return <Dialog {...muiDialog(modal)}> Hello!!! </Dialog>;
});

export const RU_LEN = NiceModal.create(() => {
  const modal = useModal();
  return <Dialog {...muiDialog(modal)}> Hello!!! </Dialog>;
});

export const RU_TA = NiceModal.create(() => {
  const modal = useModal();
  return <Dialog {...muiDialog(modal)}> Hello!!! </Dialog>;
});

export const RU_MO = NiceModal.create(() => {
  const modal = useModal();
  return <Dialog {...muiDialog(modal)}> Hello!!! </Dialog>;
});

export const RU_NIZ = NiceModal.create(() => {
  const modal = useModal();
  return <Dialog {...muiDialog(modal)}> Hello!!! </Dialog>;
});

export const RU_IVA = NiceModal.create(() => {
  const modal = useModal();
  return <Dialog {...muiDialog(modal)}> Hello!!! </Dialog>;
});

export const RU_TAM = NiceModal.create(() => {
  const modal = useModal();
  return <Dialog {...muiDialog(modal)}> Hello!!! </Dialog>;
});

export const RU_SAM = NiceModal.create(() => {
  const modal = useModal();
  return <Dialog {...muiDialog(modal)}> Hello!!! </Dialog>;
});

// dark-grey regions modals

export const RU_SAK = NiceModal.create(() => {
  const modal = useModal();
  return <Dialog {...muiDialog(modal)}> Hello!!! </Dialog>;
});

export const RU_KHA = NiceModal.create(() => {
  const modal = useModal();
  return <Dialog {...muiDialog(modal)}> Hello!!! </Dialog>;
});

export const RU_ARK = NiceModal.create(() => {
  const modal = useModal();
  return <Dialog {...muiDialog(modal)}> Hello!!! </Dialog>;
});

export const RU_MUR = NiceModal.create(() => {
  const modal = useModal();
  return <Dialog {...muiDialog(modal)}> Hello!!! </Dialog>;
});

export const RU_NGR = NiceModal.create(() => {
  const modal = useModal();
  return <Dialog {...muiDialog(modal)}> Hello!!! </Dialog>;
});

export const RU_TVE = NiceModal.create(() => {
  const modal = useModal();
  return <Dialog {...muiDialog(modal)}> Hello!!! </Dialog>;
});

export const RU_PNZ = NiceModal.create(() => {
  const modal = useModal();
  return <Dialog {...muiDialog(modal)}> Hello!!! </Dialog>;
});

export const RU_VOR = NiceModal.create(() => {
  const modal = useModal();
  return <Dialog {...muiDialog(modal)}> Hello!!! </Dialog>;
});

export const RU_AST = NiceModal.create(() => {
  const modal = useModal();
  return <Dialog {...muiDialog(modal)}> Hello!!! </Dialog>;
});

export const RU_SE = NiceModal.create(() => {
  const modal = useModal();
  return <Dialog {...muiDialog(modal)}> Hello!!! </Dialog>;
});

export const RU_BA = NiceModal.create(() => {
  const modal = useModal();
  return <Dialog {...muiDialog(modal)}> Hello!!! </Dialog>;
});

export const RU_UD = NiceModal.create(() => {
  const modal = useModal();
  return <Dialog {...muiDialog(modal)}> Hello!!! </Dialog>;
});
